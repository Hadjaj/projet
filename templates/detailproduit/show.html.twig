{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ produit.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_detailproduit_index') }}">back to list</a>

    <a href="{{ path('app_detailproduit_edit', {'id': produit.id}) }}">edit</a>

    {{ include('detailproduit/_delete_form.html.twig') }}
{% endblock %}
